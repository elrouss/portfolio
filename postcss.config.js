// TODO: Fix with classes that initially exist, but added with JS
// (perhaps, add some file with these styles?)

// import { purgeCSSPlugin } from '@fullhuman/postcss-purgecss';

// export default {
//   plugins: [
//     purgeCSSPlugin({
//       content: ['./**/*.html']
//     })
//   ]
// };
